# Docker

Ваша задача установить докер и запустить контейнер.

После установки выполните команды:

`docker build . -t server`

`docker run -p 3000:3000 server`

После чего запустите `yarn test`

## Дополнительно

Установите podman и запустите ту же последовательность команд с `podman`
